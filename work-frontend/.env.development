VITE_API_BASE_URL=http://localhost:8080/api/v1
VITE_ML_API_BASE_URL=http://localhost:8000
# Workstation service-account credentials for invisible auth bootstrap.
# These are backend-tier service credentials, not user credentials.
# The user never sees or interacts with these values.
VITE_BOOTSTRAP_USER=analyst
VITE_BOOTSTRAP_PASS=analyst123
